<div class="border rounded flex flex-col flex-grow self-start">
    <div class="border-b p-4 pl-8 font-bold text-lg">
        Suggested Friends
    </div>
    <div class="flex flex-col pt-4">
        {{!-- {{#each user.suggestedFriends}} --}}
        <div class="flex m-4 ml-8 justify-between">
            <div class="flex">
                <img class="rounded-full w-8 h-8 mr-4" src="{{user.avatar}}">
                <div>
                    <div>
                        {{user.name}}
                    </div>
                </div>
            </div>
            <div class="rounded bg-gray-300 h-8 w-8 mr-4 flex justify-center items-center">
                <i class="fa-solid fa-plus"></i>
            </div>
        </div>
        {{!-- {{/each}} --}}
    </div>
</div>